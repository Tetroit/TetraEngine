#version 460 core

in GS_OUT
{
    vec3 pos;
    vec3 normal;
    vec3 color;
} fsIn;

out vec4 Frag;

void main()
{
    float diffuseFac = dot(normalize(fsIn.normal), normalize(vec3(1,1,1)));
 Frag = vec4(diffuseFac * fsIn.color, 1.0);
}